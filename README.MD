# BTC Discord Bot

Simple Python script that fetches Bitcoin price and sends it to Discord.

## Setup

1. Create .env file:
DISCORD_WEBHOOK=your_webhook_url

2. Install dependencies:
pip install -r requirements.txt

3. Run:
python main.py